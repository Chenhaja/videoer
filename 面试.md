##### ·引入RocketMQ将视频处理相关长耗时任务全链路异步化，将60s+上传接口响应时间压缩至50ms以内。在那里体现



我先了解当前项目结构和视频处理相关代码，才能准确指出 RocketMQ 异步化改造落点。
项目里已有 RocketMQ 相关配置与 `consumer` 目录。我进一步搜索代码中 RocketMQ 生产/消费实现及视频处理链路的位置。
找到了生产/消费链路的关键文件。我来读取这些核心文件，梳理完整异步链路。
异步链路轮廓已清晰。我再追上传链路与「长耗时处理」的触发点，确认 60s+ 是从哪一段被切走的。
再看 `AiService` 中 MQ 相关用法与消费端触发重活的链路，同时查 git 提交历史确认改造背景。
查看相关文档与未跟踪目录，确认这条改造叙事对应的语境。
查看 docs 里的面试问答文档，看是否已就这类描述做过梳理。
查看 RocketMQ 引入历史与 Spring 配置类，确认「改前同步、改后异步」的演变是否可从提交历史佐证。
查看转写任务的投递方式（是否同样走 MQ），并确认两个未跟踪目录与本问题的关系。
快速确认管理端死信/重放入口与失败任务表，补全闭环证据。
这条描述在仓库里的落点我全部核对完了，结论分两部分：**能对上的实现位置**和**必须修正的说法**。

## 一、RocketMQ 异步链路在代码中的真实位置

"全链路异步化"在本仓库 = 分析任务离开 Web 请求线程，转入 MQ 消费线程。链路各环节如下：

**1. 入口：受理接口只投递、秒回 202，不干活**

```100:114:server/src/main/java/com/example/server/controller/AnalysisController.java
AnalysisController.javaL100-L114
@PostMapping("/ai")
public ResponseEntity<Result<Void>> aiAnalyze(...)
    ...
    return submissionResponse(dispatchService.submit(mediaFile, normalizedGoal, null, analysisMode));
```

```275:283:server/src/main/java/com/example/server/controller/AnalysisController.java
case ACCEPTED -> ResponseEntity.accepted().body(Result.ok());
```
投递成功即返回 `202 Accepted`——这就是"快速返回"的直接体现（追问重跑 `/agent-revise`、文字提取 `/analysis/transcribe` 同为 202 受理）。

**2. 生产者：限流 + 幂等 + 投递，毫秒级完成**

```63:101:server/src/main/java/com/example/server/service/AnalysisDispatchService.java
AnalysisDispatchService.javaL63-L101
// Redis setIfAbsent 做(内容指纹+目标摘要)幂等；RRateLimiter 双维度限流；
rocketMQTemplate.convertAndSend(analysisTopic, new AnalysisTaskMsg(...));
// 随后发布 QUEUED 事件 → SSE
```

**3. 消费者：锁、去重、真正触发重活**

VideoAnalysisConsumer.javaL95-L108

```29:41:server/src/main/java/com/example/server/consumer/VideoAnalysisConsumer.java
VideoAnalysisConsumer.javaL29-L41
@RocketMQMessageListener(topic=video-analysis-topic, consumerGroup=..., maxReconsumeTimes=2)
```

```82:141:server/src/main/java/com/example/server/consumer/VideoAnalysisConsumer.java
VideoAnalysisConsumer.javaL82-L141
// Redisson 分布式锁幂等 → Redis attempts 计数 → aiService.asyncAnalyze() 触发重活
```
分钟级重活（即"60s+"的来源）都在消费线程执行，见 `AiService.asyncAnalyze` → `resolveContext` → `VideoContextService.build`（ffmpeg 抽帧、逐 60s 切片 ASR、OCR 两路并行，`AiService.java:30-34` 注释自述"ASR+关键帧OCR是分钟级作业"），Web 线程已被完全释放。

**4. 失败兜底闭环**：`VideoAnalysisConsumer.java:164-204` 区分永久/瞬时失败（瞬时失败抛异常交 MQ 重投、重投耗尽转死信主题 `video-analysis-dead-topic`）；`FailedAnalysisTaskService` 落失败台账，`AdminController.java:38-46` 提供管理端重放，重放同样走 `convertAndSend`。

**5. 状态闭环（前端感知"已完成异步受理"）**：SSE `/analysis/analysis-events` 与轮询 `/analysis/analysis-status`（`AnalysisController.java:188-215`）；前端收到 202/409 后接管 SSE 流（`client/src/useAnalysisWorkspace.js:311-329`），全程不依赖同步返回。

**6. 基础设施**：`docker-compose.yml:58-82`（namesrv+broker，RocketMQ 5.3.4）、`application.properties:71-77`（topic/消费组配置）、`server/pom.xml:93-97`（rocketmq-spring-boot-starter 2.3.0）、`rocketmq/broker.conf`。

## 二、需要修正的表述（诚实性审计）

"**上传接口** 60s+ → 50ms"这半句在本仓库**没有字面落点**：

- 真正的 MQ 边界是**分析受理**（`/analysis/ai` 202），不是 `/media/upload`。上传走分片→MinIO→合并（`MediaController` + `MediaIngestService`），本来就不含 ASR/OCR/AgentLoop，也不经过 MQ；
- 代码里**没有任何 50ms / 60s 的压测埋点或基准数据**，这个数字无法复现——`docs/interview-qa-v2.md:476` 也明确记录："最新版简历已经删除 60 秒压缩到 50ms 的数字……更稳的表达是接口快速返回任务状态、长耗时处理转入 MQ，并通过状态和 SSE 完成业务闭环"。

**建议表述改为**："引入 RocketMQ 将分钟级视频分析链路（ASR+OCR+AgentLoop）异步化，分析受理接口 202 秒回、长耗时任务转入独立消费线程，配合 Redis 幂等、Redisson 锁与死信台账保证至少一次执行且失败可重放。"——这句话的每个分句都能在上述代码位置逐条对上。



##### 设计Redisson+WatchDog分布式锁，基于MD5实现内容级去重。解决视频转码长耗时导致的锁过期问题，在测试并发场景下成功拦截重复提交，有效避免算力浪费，节省不必要的AI Token开销。代码在哪里体现

这次同样先做精确核对：找出 Redisson 锁、MD5 内容指纹与内容级去重的代码落点，再判断哪些能对上、哪些要修正。先并行读取关键文件。
再看各 `getLock`/`tryLock` 调用点的参数（是否有 leaseTime 决定是否走 WatchDog），以及 Redisson 依赖与配置。
核对测试代码与文档审计中对该描述的表述。
核对完毕。这条描述的大部分环节有代码落点，但有两个分句（"测试并发场景""节省 Token"）在仓库内**没有证据**，需要按你 `docs/interview-qa-v2.md` 的审计口径改写。分两部分说明：

## 一、能对上的代码位置

**1. Redisson 分布式锁（无 leaseTime → WatchDog 自动续期）**

三把锁均调用 `tryLock()`/`tryLock(waitTime)` 且**不带 leaseTime**——这正是 Redisson 走 WatchDog（持锁线程存活期间每 10s 续期，默认 30s）的前提：

```159:190:server/src/main/java/com/example/server/service/AiService.java
AiService.javaL159-L190
// 内容级预处理锁：分钟级 ASR+OCR 构建期间持锁
RLock contextLock = redissonClient.getLock(AnalysisTaskKeys.contextLock(contentHash));
locked = contextLock.tryLock(CONTEXT_LOCK_WAIT_SECONDS, TimeUnit.SECONDS);
...
if (!locked) { ... throw new IllegalStateException("同一视频的上下文正在构建中，稍后重试"); }
...
finally { if (locked && contextLock.isHeldByCurrentThread()) contextLock.unlock(); }
```
`CONTEXT_LOCK_WAIT_SECONDS = 300`（`AiService.java:34`），代码注释自述"ASR + 关键帧 OCR 是分钟级作业"——长耗时临界区的锁由 WatchDog 兜底不过期；等锁超时则抛异常交 RocketMQ 跨时间重投（`:175-183`）。

```95:108:server/src/main/java/com/example/server/consumer/VideoAnalysisConsumer.java
VideoAnalysisConsumer.javaL95-L108
// 消费侧幂等锁：同一(内容+目标)同时只允许一个执行者，重复消息直接丢弃
RLock lock = redissonClient.getLock(AnalysisTaskKeys.lock(contentHash, goalDigest));
acquired = lock.tryLock();
if (!acquired) { log.info(...); return; }  // 不重复执行 = 不重复烧 ASR/OCR/LLM
```

```115:120:server/src/main/java/com/example/server/service/ChunkUploadService.java
ChunkUploadService.javaL115-L120
// 分片合并锁：并发 complete 只允许一个进入合并区，其余 409
RLock mergeLock = redissonClient.getLock("lock:upload:merge:" + uploadId);
if (!mergeLock.tryLock()) { throw new BusinessException(ErrorCode.CONFLICT, ...); }
```

**2. MD5 内容指纹的产生与存储**

```66:76:server/src/main/java/com/example/server/service/MediaService.java
MediaService.javaL66-L76
// 流式计算 MD5（上传/外链两种来源）
public String calculateMd5(MultipartFile file) ...
public String calculateMd5(File file) ...
```
```223:231:server/src/main/java/com/example/server/service/MediaService.java
MediaService.javaL223-L231

private String calculateMd5(InputStream inputStream) ...  // 8KB 缓冲流式读，不整文件进内存
```
落库到 `media_files.content_hash`（`MediaService.java:87-104`）并缓存 `media:md5:<id>`；`contentHash(mediaId)`（`:131-143`）读取。`AnalysisTaskKeys.normalizeContentHash`（`AnalysisTaskKeys.java:19-24`）只认 32 位 hex 的合法 MD5，否则退化为 `media-<id>`，防止脏数据污染指纹键。

**3. 基于内容指纹的"去重"（三层，各自不同职责）**

- 提交入口去重：`activeKey = contentHash + goalDigest` 用 `setIfAbsent` 拦截重复提交 → `DUPLICATE` → HTTP 409（`AnalysisDispatchService.java:71-74`；`AnalysisController.java:280-281`）。注意：**这一层是 Redis SETNX，不是 Redisson 锁**。
- 消费侧内容级复用：同 MD5 的视频换目标/换用户重复分析时，跳过整段 ASR+OCR：

```194:214:server/src/main/java/com/example/server/service/AiService.java
AiService.javaL194-L214
private VideoContext reuseContentContext(...) {
    Long ownerMediaId = contextOwner(contentHash);   // 查"哪个 mediaId 已产出该内容上下文"
    ...
    checkpointService.saveContext(mediaFile.getId(), localized);   // 直接把别人构建的上下文挂到本 mediaId
    telemetry.increment(traceId, "contextContentReuses", 1);
}
```
构建成功后登记归属 `rememberContextOwner(contentHash, mediaId)`（`:233-234`，键定义见 `AnalysisTaskKeys.java:80-90`）；"先落盘、再登记归属"（`:233` 注释）保证索引永远指向可读上下文。
- 结果级复用：消费者发现 `completedKey`（内容级作用域）有已完成结果时，跨 mediaId 直接复用 AnalysisResult（`VideoAnalysisConsumer.java:124-138`）。

**4. "拦截重复提交"的并发防线汇总**

submit 侧 SETNX（`AnalysisDispatchService:72-74`）→ 409 秒回；消费侧 Redisson 锁丢弃并发重复消息（`VideoAnalysisConsumer:104-108`）；分片合并侧 merge 锁 409（`ChunkUploadService:118-120`）；MQ 重投/重复消费由 `attemptsKey` + `maxReconsumeTimes=2` + `completedKey`/Checkpoint 收敛（`VideoAnalysisConsumer:46,113-115`）。

## 二、仓库内没有证据、必须修正的分句

按你 `docs/interview-qa-v2.md` 的审计结论（该文正是为此类简历句写的口径），以下三点代码撑不住：

1. **"在测试并发场景下成功拦截重复提交"**：`server/src/test` 只有 3 个单测（`AgentCheckpointServiceTest`、`AgentExecutionBudgetTest`、`EvidenceVerificationServiceTest`），**没有任何并发/压测代码或结果报告**。审计原文（`docs/interview-qa-v2.md:330`）明确写"仓库没有实际压测和回归结果……不能写'降低多少 Token'或'恢复成功率多少'"。
2. **"有效避免算力浪费、节省 AI Token 开销"**：复用逻辑确实会省（`contextContentReuses`、`contextLockContentions` 计数指标已埋，`AiService.java:179,210`），但仓库没有埋点对比报告，"节省多少"无法复现。
3. **"视频转码长耗时"**：本仓库没有"转码"步骤，长耗时是 VideoContext 构建（ASR+OCR，分钟级）。另外按 `docs/interview-qa-v2.md:442`，"内容级去重"只发生在**解析/Agent 消费阶段**，上传阶段仍是按 uploadId 各自存对象，**没有文件级秒传**，表述别越界。

**可站得住的表述建议**："基于 Redisson 分布式锁（不设 leaseTime，由 WatchDog 自动续期）与 Redisson 限流器，配合内容 MD5 指纹 + 目标摘要双键，在任务提交、MQ 消费、分片合并三层拦截重复/并发操作；同一内容重复分析时复用既有 VideoContext 与结果，跳过重复 ASR/OCR/Agent 调用。"——每个分句都对应上面的代码行。



##### \- 采取分片上传与断点续传机制，通过Redis记录分片上传状态，解决弱网环境下的连接中断超时问题，确保GB级大文件在不稳定网络下的传输可靠性。代码体现在哪里

继续核对第三条：分片上传与断点续传。先定位上传 Controller、`ChunkUploadService` 全貌，以及前端分片上传实现。
后端关键文件已确认。继续读接口层与前端的断点续传实现。
前端调用方确认（重选文件后的续传提示），再查 UI 侧如何接续传进度。
核对完毕。这条是四条描述里代码支撑最完整的一条，后端接口、Redis 状态、前端切片与续传闭环都在。分两部分说明。

## 一、代码落点（后端 + 前端两条链）

**1. 后端：四个接口组成分片协议**（`MediaController.java`）

```65:94:server/src/main/java/com/example/server/controller/MediaController.java
@PostMapping("/init-upload")     // 初始化任务，返回 uploadId
@GetMapping("/upload-status")   // 查询已上传分片序号集合 → 断点续传的“核对”依据
@PostMapping("/upload-chunk")   // 上传单个分片
@PostMapping("/complete-upload")// 全部分片就绪后触发合并
```

**2. Redis 记录分片状态与常量边界**（`ChunkUploadService.java`）

```38:41:server/src/main/java/com/example/server/service/ChunkUploadService.java
private static final String UPLOAD_KEY_PREFIX = "upload:chunked:";
private static final long MAX_CHUNK_BYTES = 5L * 1024 * 1024;   // 单片 5MB
private static final int MAX_TOTAL_CHUNKS = 410;                // 覆盖约 2GB
```

- 初始化：`upload:chunked:<uploadId>` Hash 存 filename/totalChunks/userId，TTL 1 天（`:58-73`）；
- 每片成功：先写 MinIO 对象 `chunk-uploads/<uploadId>/part-N`，再把序号 `SADD` 进 `<uploadKey>:parts` 集合（`:101-110`）——**Set 里只存在成功分片**，这就是断点续传的真相源；
- 状态查询：`uploadedChunks()` 把 Set 读出返回（`:75-83`），前端据此只传差集。

**3. 合并接口的幂等与一致性**（`ChunkUploadService.complete`，`:113-160`）

- Redisson 合并锁防并发 complete → 409（`:115-120`）；
- `completedUpload` 先查 `<uploadKey>:completed`，已完成直接返回同一条媒体记录，不重复生成（`:123-124,162-171`）；
- 校验已传分片数 == totalChunks 才允许合并（`:129-133`）；
- 按序号顺序拼接的同时用 `DigestOutputStream` 流式计算 MD5（`:135-148`），合并结果落 MinIO 后把 MD5 随媒体记录入库——**完整文件一致性由 MD5 兜底校验**；
- "先写 completedKey 再清现场"（`:149-153`），客户端重试 complete 也不会插第二条记录；`cleanup()` 删除临时分片对象与 Redis 键（`:193-207`）。

**4. 前端：分片 + 并发 + 指数退避重试 + 断点续传**（`client/src/chunkUpload.js`）

```3:10:client/src/chunkUpload.js
const CHUNK_SIZE = 5 * 1024 * 1024      // 与后端 5MB 对齐
const UPLOAD_CONCURRENCY = 3            // 3 并发
const CHUNK_MAX_ATTEMPTS = 4            // 单片最多重试 4 次
const CHUNK_RETRY_BASE_MS = 800         // 指数退避 800→8000ms（带抖动）
```

- **弱网分片级重试**：单片失败按指数退避重试，只重试网络抖动/408/429/5xx，4 次仍败才终止整次上传（`:253-271`、`isRetriable` `:293-298`）；
- **断点续传核心** `resolveUploadSession`（`:204-240`）：凭据存 localStorage（键=文件名+大小+修改时间，`:55-74`）；重新选择同一文件时先 GET `/media/upload-status` 拿已传集合，**只上传差集**；网络故障时"进度未知≠进度失效"，明确保留凭据让用户稍后继续（`:213-215`）；只有服务端返回 400 明确"不存在/已过期"才丢弃本地进度（`:226-234`）；
- **3 并发 worker 池**（`:145-172`）+ 滑动窗口实时测速与 ETA（`:318-335`）；
- **取消不丢进度**：`AbortSignal` 取消抛 `UploadAbortedError`，成功前不删除续传凭据（`:92`、`forgetUploadProgress` 仅在成功时调用 `:187`）。

**5. UI 闭环**（`client/src/App.vue`）：上传中断后展示"进度已保留，可点继续上传"提示条，选择"继续上传/重新开始"（`:127-131,748-761`）；进度条显示"已续传：跳过 N 个此前完成的分片"（`buildUploadWarning` `:678-686`）。

## 二、按你的审计口径必须收紧的说法

1. **"解决连接中断超时问题"**：代码层面是**分片级重试 + 整任务断点续传**，没有显式配置 HTTP 连接/读超时，也不是传输层（TCP/QUIC）层面的可靠性；而且 `docs/interview-qa-v2.md:400` 明确"当前仓库没有这组（断网/重传/并发）测试结果，因此只保留定性亮点"。
2. **"GB 级大文件"**：成立但有硬上限——`5MB × 410 ≈ 2GB`（`ChunkUploadService:39-40`），文档 `:364-366` 也要求"不能声称支持任意 10GB"。
3. **"确保传输可靠性"的边界**（都是审计里点名的）：Redis 任务 TTL 1 天，过期后需重新初始化（`:71`，文档 `:374`）；跨 MinIO/MySQL/Redis 无事务，进程在"完整文件已写、DB 记录未写"间隙宕机可能留孤立对象（文档 `:380`）；Redis 全量丢失不会自动扫描 MinIO 重建 Set（文档 `:384-388`）。

**可站得住的表述**："采用 5MB 分片、3 路并发的分片上传协议，后端用 Redis Set 记录已成功分片作为续传真相源；单分片失败指数退避重试（上限 4 次），断网/中断后重新选择同一文件只重传差集，取消上传保留进度可继续；合并阶段以 Redisson 锁防并发、completedKey 幂等防重复建记录、流式 MD5 校验完整文件，覆盖约 2GB 视频。"——每个分句都能对上上面代码行。



##### \- 基于Redis实现令牌桶限流，设置每秒请求上限，遏制恶意请求带来的高昂AI Token开销，保障服务可用性。这个代码体现在哪

我来在代码库中查找基于 Redis 的令牌桶限流实现。
找到相关线索了，文档提到用的是 Redisson 的 `RRateLimiter`。让我定位实际代码。
核心实现在 `AnalysisDispatchService.java`。让我读取完整逻辑。
现在查找 `RATE_LIMITED` 到 HTTP 429 的映射，以及限流入口的调用点。
让我确认 Redisson 配置，完善整体链路。
找到了。需要先说明一个关键点：**实际代码并不是"手写 Redis Hash + Lua 令牌桶"，也没有"每秒"上限**，而是用 **Redisson 的 `RRateLimiter`（令牌桶算法，状态存 Redis）**，粒度是**每分钟**。相关文档（`docs/interview-qa-v2.md:21`）也特意澄清过这个历史偏差。

## 核心实现位置

**1. 主限流逻辑 —— `AnalysisDispatchService.java`**

```29:30:server/src/main/java/com/example/server/service/AnalysisDispatchService.java
AnalysisDispatchService.javaL29-L30
private static final int USER_REQUESTS_PER_MINUTE = 5;
private static final int GLOBAL_REQUESTS_PER_MINUTE = 30;
```

```132:141:server/src/main/java/com/example/server/service/AnalysisDispatchService.java
    AnalysisDispatchService.javaL132-L141
	private boolean tryAcquireQuota(Long userId) {
        RRateLimiter userLimiter = redissonClient.getRateLimiter("limit:ai:user:" + userId);
        userLimiter.trySetRate(RateType.OVERALL, USER_REQUESTS_PER_MINUTE, 1, RateIntervalUnit.MINUTES);
        if (!userLimiter.tryAcquire()) return false;

        RRateLimiter globalLimiter = redissonClient.getRateLimiter("limit:ai:global");
        globalLimiter.trySetRate(
                RateType.OVERALL, GLOBAL_REQUESTS_PER_MINUTE, 1, RateIntervalUnit.MINUTES);
        return globalLimiter.tryAcquire();
    }
```

**用户级 + 全局级双重令牌桶**，两个许可都拿到才放行；`RRateLimiter` 内部就是令牌桶算法，Redis 存状态，多实例共享同一配额。

**2. 提交入口调用限流 —— 同文件 `submit()`**

```76:80:server/src/main/java/com/example/server/service/AnalysisDispatchService.java
AnalysisDispatchService.javaL76-L80
		try {
            if (!tryAcquireQuota(mediaFile.getUserId())) {
                redisTemplate.delete(activeKey);
                return SubmissionResult.RATE_LIMITED;
            }
```

限流点放在 **MQ 投递之前**，避免无效任务先进队列再排队烧资源；被限流时回滚 `activeKey`（幂等键）。

**3. 追问/证据检索复用同一配额 —— `requireAiQuota()`**

```119:130:server/src/main/java/com/example/server/service/AnalysisDispatchService.java
AnalysisDispatchService.javaL119-L130
	public void requireAiQuota(Long userId) {
        try {
            if (!tryAcquireQuota(userId)) {
                throw new BusinessException(ErrorCode.RATE_LIMITED, "AI 请求过于频繁，请稍后再试");
            }
        } catch (BusinessException e) {
            throw e;
        } catch (RuntimeException e) {
            log.warn("ai_rate_limiter_unavailable userId={}", userId, e);
            throw new BusinessException(ErrorCode.SERVICE_UNAVAILABLE, "AI 服务限流器暂不可用，请稍后再试");
        }
    }
```

调用点：`AnalysisController.java:128`（追问）和 `AnalysisController.java:141`（证据检索），防止绕过成本护栏。注意这里是 **fail-closed**：Redis 挂了直接拒绝，而非放行。

**4. 自动意图路由的独立配额 —— `ModeRouter.java`**

```78:90:server/src/main/java/com/example/server/service/mode/ModeRouter.java
ModeRouter.javaL78-L90
	private boolean tryAcquireQuota(Long userId) {
        if (userId == null) return false;
        try {
            RRateLimiter userLimiter = redissonClient.getRateLimiter(
                    "limit:ai:route:user:" + userId);
            userLimiter.trySetRate(
                    RateType.OVERALL, USER_ROUTES_PER_MINUTE, 1, RateIntervalUnit.MINUTES);
            if (!userLimiter.tryAcquire()) return false;

            RRateLimiter globalLimiter = redissonClient.getRateLimiter("limit:ai:route:global");
            globalLimiter.trySetRate(
                    RateType.OVERALL, GLOBAL_ROUTES_PER_MINUTE, 1, RateIntervalUnit.MINUTES);
            return globalLimiter.tryAcquire();
        } catch (RuntimeException e) {
```

路由限流是**独立配额**（10/min 用户、60/min 全局），且配额不足时**降级为 GENERAL 继续**，不让增强能力拖垮主流程——与上面主链路的 fail-closed 策略相反。

**5. HTTP 429 映射**

```20:20:server/src/main/java/com/example/server/common/ErrorCode.java
    RATE_LIMITED(42900, HttpStatus.TOO_MANY_REQUESTS),
```

```277:282:server/src/main/java/com/example/server/controller/AnalysisController.java
AnalysisController.javaL277-L282
		return switch (result) {
            case ACCEPTED -> ResponseEntity.accepted().body(Result.ok());
            case RATE_LIMITED -> throw new BusinessException(ErrorCode.RATE_LIMITED, "系统繁忙，请稍后再试");
            case DUPLICATE -> throw new BusinessException(ErrorCode.CONFLICT, "相同视频和分析目标正在处理中");
            case FAILED -> throw new BusinessException(ErrorCode.INTERNAL_ERROR, "任务提交失败");
        };
```

## 依赖

```99:103:server/pom.xml
		<dependency>
			<groupId>org.redisson</groupId>
			<artifactId>redisson-spring-boot-starter</artifactId>
			<version>3.52.0</version>
		</dependency>
```

`RedissonClient` 由 `redisson-spring-boot-starter` 自动装配，项目里没有手写的 Redisson Config 类。

## 需要提醒你的两点

1. **算法**：用的是 Redisson `RRateLimiter`（令牌桶），不是自研 Lua 脚本。文档 `docs/interview-qa-v2.md` 明确纠正过"旧文档写手搓 Redis Hash + Lua 令牌桶"的说法。
2. **粒度**：代码是**每分钟 5 次（用户）/ 30 次（全局）**，并非"每秒请求上限"。如果简历/描述里写的是"每秒上限"，与当前代码不符，建议改成"用户级与全局级令牌桶（每分钟粒度）"以免面试被追问时对不上。另外这是**请求级**限流，不能精确对应模型 Token 消耗（长视频和短视频成本差异很大），文档里也承认了这个局限。



##### 应对三方API网络抖动使用指数退避重试机制，兜底第三方API调用失败场景，显著提升任务执行成功率。体现在代码那里

我来定位指数退避重试的实现代码。
找到了两处核心实现。让我读取完整的重试逻辑。
让我确认常量定义，以及消费者侧的重试兜底。
两处核心实现，都在第三方调用的工具类里，采用**指数退避（1s → 2s，基数 2）**：

## 1. 大模型调用重试 —— `DeepSeekUtils.java`

```390:417:server/src/main/java/com/example/server/utils/DeepSeekUtils.java
    private String chat(String stage, String prompt) {
        RuntimeException lastError = null;
        for (int attempt = 0; attempt < MAX_MODEL_ATTEMPTS; attempt++) {
            long started = System.nanoTime();
            try {
                String response = invokeModel(prompt);
                if (response == null || response.isBlank()) {
                    throw new RetriableException("模型返回空响应");
                }
                telemetry.modelCall(stage, SYSTEM_POLICY + "\n" + prompt, response,
                        inputPricePerMillion, outputPricePerMillion, started);
                return response;
            } catch (RuntimeException e) {
                lastError = e;
                telemetry.incrementCurrent("modelCallFailures", 1);
                boolean retriable = isRetriableModelFailure(e);
                if (!retriable || attempt == MAX_MODEL_ATTEMPTS - 1) {
                    telemetry.failCurrentStage(stage, started);
                    if (!retriable) {
                        throw new IllegalArgumentException("模型请求不可重试", e);
                    }
                    break;
                }
                waitBeforeRetry(attempt);
            }
        }
        throw new IllegalStateException("模型调用达到最大重试次数", lastError);
    }
```

**指数退避本体：**

```465:472:server/src/main/java/com/example/server/utils/DeepSeekUtils.java
    private void waitBeforeRetry(int attempt) {
        try {
            Thread.sleep(1_000L << attempt);
        } catch (InterruptedException e) {
            Thread.currentThread().interrupt();
            throw new IllegalStateException("模型重试被中断", e);
        }
    }
```

`1_000L << attempt` 即位运算实现 2 的幂次退避：attempt=0 → 1s，attempt=1 → 2s。`MAX_MODEL_ATTEMPTS = 3`（`DeepSeekUtils.java:35`）。

**可重试判定（区分抖动 vs 确定性失败）：**

```450:463:server/src/main/java/com/example/server/utils/DeepSeekUtils.java
    private boolean isRetriableModelFailure(Throwable error) {
        Throwable current = error;
        for (int depth = 0; current != null && depth < MAX_CAUSE_DEPTH; depth++) {
            if (current instanceof NonRetriableException) return false;
            if (current instanceof RetriableException) return true;
            if (current instanceof HttpException httpException) {
                int status = httpException.statusCode();
                return status == 408 || status == 429 || status >= 500;
            }
            if (current.getCause() == current) break;
            current = current.getCause();
        }
        return false;
    }
```

只对 **408 / 429 / 5xx** 和超时、线程池繁忙、空响应等临时故障重试；沿 cause 链查找，避免只看到外层包装异常。超时封装在 `invokeModel()`（`DeepSeekUtils.java:419-448`）。

## 2. ASR 语音识别重试 —— `AliyunAsrUtils.java`

```44:64:server/src/main/java/com/example/server/utils/AliyunAsrUtils.java
    public String audioToText(String filePath) {
        File file = new File(filePath);
        // 这里的音频是本流水线上一步用 ffmpeg 切出来的，缺失属于「意外状态」而非「调用方参数错误」，
        // 重跑流水线可以重新生成，因此用 IllegalStateException 保持它可重试，
        // 不要和下面表示「请求本身不合法、重试无意义」的 IllegalArgumentException 混用。
        if (!file.isFile()) throw new IllegalStateException("ASR audio file does not exist");

        Exception lastError = null;
        for (int attempt = 0; attempt < MAX_ATTEMPTS; attempt++) {
            try {
                String text = execute(file);
                if (text == null || text.isBlank()) throw new IllegalStateException("ASR 返回空文本");
                return text.trim();
            } catch (IOException e) {
                lastError = e;
                log.warn("asr_attempt_failed attempt={} file={}", attempt + 1, file.getName(), e);
                if (attempt < MAX_ATTEMPTS - 1) waitBeforeRetry(attempt);
            }
        }
        throw new IllegalStateException("ASR 调用失败，已达到最大重试次数", lastError);
    }
```

**同样的退避实现：**

```94:101:server/src/main/java/com/example/server/utils/AliyunAsrUtils.java
    private void waitBeforeRetry(int attempt) {
        try {
            Thread.sleep(1_000L << attempt);
        } catch (InterruptedException e) {
            Thread.currentThread().interrupt();
            throw new IllegalStateException("ASR retry interrupted", e);
        }
    }
```

**失败分类：**

```85:90:server/src/main/java/com/example/server/utils/AliyunAsrUtils.java
            if (response.code() == 429 || response.code() >= 500) {
                throw new RetryableAsrException("ASR transient HTTP " + response.code());
            }
            // 非 429/5xx 的失败源于请求本身（参数、鉴权、音频格式不支持），重投多少次都不会变好。
            // 用 IllegalArgumentException 让上层能识别为「不可重试」，避免整条流水线空转重试。
            throw new IllegalArgumentException("ASR request rejected with HTTP " + response.code());
```

`RetryableAsrException extends IOException`（`AliyunAsrUtils.java:103-107`），只在 429/5xx 时抛出并进入重试；其他 4xx 抛 `IllegalArgumentException` 直接上抛，不浪费重试。`MAX_ATTEMPTS = 3`（`AliyunAsrUtils.java:24`），另外 OkHttp 层还开了 `retryOnConnectionFailure(true)`（`AliyunAsrUtils.java:33`）兜底 TCP 连接抖动。

## 3. 更外层的兜底 —— MQ 消费重试 + 死信

指数退避是**单次调用内**的抖动兜底；整条流水线级别的兜底在 `VideoAnalysisConsumer.java`：

```33:46:server/src/main/java/com/example/server/consumer/VideoAnalysisConsumer.java
        // maxReconsumeTimes 计的是「重投次数」，2 次重投 = 最多 3 次投递，与下面的
        // MAX_DELIVERY_ATTEMPTS（计投递次数）对齐。不设的话，一旦异常发生在 Redis 计数之前，
        // 应用侧上限失效，会退化成 MQ 默认的 16 次重投空转。
        maxReconsumeTimes = 2)
public class VideoAnalysisConsumer implements RocketMQListener<AnalysisTaskMsg> {
    /** 应用级投递上限，需与注解上的 maxReconsumeTimes 保持一致。 */
    private static final int MAX_DELIVERY_ATTEMPTS = 3;
```

超过 3 次投递仍失败则写入失败台账并转投死信主题（`VideoAnalysisConsumer.java:179-201`），配合 `isPermanentFailure()`（`VideoAnalysisConsumer.java:303-317`）识别永久失败，避免确定性错误空转重试。

## 小结（三层防护）

| 层级          | 位置                         | 机制                                      |
| ------------- | ---------------------------- | ----------------------------------------- |
| 单次 LLM 调用 | `DeepSeekUtils.chat`         | 指数退避 1s→2s，最多 3 次，仅 408/429/5xx |
| 单次 ASR 调用 | `AliyunAsrUtils.audioToText` | 指数退避 1s→2s，最多 3 次，仅 429/5xx     |
| 整条流水线    | `VideoAnalysisConsumer`      | RocketMQ 重投最多 3 次 + 死信主题兜底     |

**提示**：这里的"指数退避"严格说是 `1s → 2s` 两档（`MAX_ATTEMPTS=3` 只有 2 次等待），并且**没有加随机抖动（jitter）**，也**没有遵循 `Retry-After` 响应头**。如果简历/面试强调"指数退避"，可以说"基数 2 的指数退避"，但不要说成"带抖动的指数退避"，以免被追问时对不上。文档 `docs/interview-qa-v2.md:522` 也是这个口径。





是怎么确定关键帧来进行ocr的

关键帧的选择是**两级筛选**：先在 ffmpeg 解码阶段用场景变化挑，再在 Java 侧用感知哈希去重，最后才送 OCR。

## 第一级：ffmpeg 的 `select` 表达式

```211:216:server/src/main/java/com/example/server/service/VideoContextService.java
        runCommand(List.of(
                "ffmpeg", "-y", "-i", videoPath,
                "-vf", "select=eq(n\\,0)+gt(scene\\,0.35)+gte(t-prev_selected_t\\,30),showinfo",
                "-vsync", "vfr",
                frameDir.resolve("frame_%06d.jpg").toString()
        ), timestamps);
```

`select` 里三个条件用 `+`（逻辑或）连接，任一成立就吐帧：

| 条件                        | 含义               | 作用                                           |
| :-------------------------- | :----------------- | :--------------------------------------------- |
| `eq(n,0)`                   | 第 0 帧            | 保证封面/片头不漏                              |
| `gt(scene,0.35)`            | 场景变化分 > 0.35  | 抓镜头切换、翻页、PPT 换页                     |
| `gte(t-prev_selected_t,30)` | 距上次选中 ≥ 30 秒 | 兜底：长时间静态画面（如固定机位讲课）强制采样 |

注意 `scene` 是 ffmpeg 按帧间差异算的分数（0~1），**不是 GOP 的 I 帧**。所以"关键帧"在这里的语义是"视觉上有代表性的帧"，跟编码层面的关键帧无关。

另外两个配套参数很关键：
- `-vsync vfr` — 保持可变帧率，每个写盘的帧保留自己的真实时间戳。否则 ffmpeg 会复制帧填充时间轴，导致文件和时刻对不上
- `showinfo` — 把每帧信息打到日志，用来回填时间戳

时间戳的回填靠正则：

```334:340:server/src/main/java/com/example/server/service/VideoContextService.java
    private void appendTimestamp(String line, List<Long> timestamps) {
        if (!line.contains("showinfo")) return;
        Matcher matcher = PTS_TIME.matcher(line);
        if (matcher.find()) {
            timestamps.add((long) (Double.parseDouble(matcher.group(1)) * 1000));
        }
    }
```

`PTS_TIME = Pattern.compile("pts_time:([0-9.]+)")` 抓秒数再乘 1000 存毫秒。文件名用 `%06d` 补零，所以 `Files.list(...).sorted()` 的字典序正好等于时间序——这是刻意设计。

## 第二级：dHash 去重

ffmpeg 只保证"变了"，不保证"变得有意义"。比如一段缓慢推镜、或者画面里只有一个人在动，`scene` 分数会持续高于阈值，产生大量视觉上几乎相同的帧。所以在 OCR 之前再过一道感知哈希：

```226:232:server/src/main/java/com/example/server/service/VideoContextService.java
        for (int i = 0; i < frameFiles.size(); i++) {
            long imageHash = differenceHash(frameFiles.get(i).toFile());
            if (previousHash != null && Long.bitCount(previousHash ^ imageHash) <= 5) {
                continue;
            }
            previousHash = imageHash;
            long timestampMs = i < timestamps.size() ? timestamps.get(i) : i * FALLBACK_FRAME_INTERVAL_MS;
```

`differenceHash` 是标准 dHash：

```286:305:server/src/main/java/com/example/server/service/VideoContextService.java
    private long differenceHash(File imageFile) throws Exception {
        BufferedImage source = ImageIO.read(imageFile);
        if (source == null) return 0;
        BufferedImage scaled = new BufferedImage(9, 8, BufferedImage.TYPE_BYTE_GRAY);
        ...
        long hash = 0;
        for (int y = 0; y < 8; y++) {
            for (int x = 0; x < 8; x++) {
                hash <<= 1;
                if (scaled.getRGB(x, y) > scaled.getRGB(x + 1, y)) hash |= 1;
            }
        }
        return hash;
    }
```

缩成 9×8 灰度图，逐行比较水平相邻像素的亮度，得到 64 位哈希。然后比汉明距离：`Long.bitCount(previousHash ^ imageHash) <= 5` 就判定为视觉重复，跳过 OCR。

**这里有个容易被忽略的细节**：`previousHash` 只在 `continue` 未触发时更新，所以比较对象是**上一个被接受的帧**，不是上一张原始帧。这是链式比较——好处是能跳过"与最近一份有效证据重复"的帧；局限是对**缓慢渐变**不敏感（相邻两帧差异都小于阈值，但累积起来已经面目全非，中途的帧会被全部跳过）。

这三个阈值全是硬编码常量，不可配置：`scene 0.35`、`30` 秒、汉明距离 `5`。

## OCR 与后续

通过筛选的帧才真正调 OCR：

```234:236:server/src/main/java/com/example/server/service/VideoContextService.java
            try {
                telemetry.increment(traceId, "ocrCalls", 1);
                ocrText = ocrUtils.recognize(frameFiles.get(i).toFile());
```

`OcrUtils` 是 tesseract 子进程：

```31:35:server/src/main/java/com/example/server/utils/OcrUtils.java
            process = new ProcessBuilder(
                    ocrCommand, image.getAbsolutePath(), "stdout", "-l", "chi_sim+eng")
                    .redirectErrorStream(true)
                    .redirectOutput(output.toFile())
                    .start();
```

`-l chi_sim+eng` 中英双语是**硬编码**的，只有可执行文件路径能配（`tool.ocr.command`，默认 `tesseract`）。单帧超时 2 分钟。

容错策略是"单帧失败不致命"：
- OCR 失败 → 记 `ocrFrameFailures`，`continue`（该帧直接丢弃，不进结果）
- 上传 MinIO 失败 → 记 `frameUploadFailures`，URL 退化成 `videoPath#timestampMs=xxx`
- 只有**全部帧都失败**才抛异常

```259:261:server/src/main/java/com/example/server/service/VideoContextService.java
        if (result.isEmpty() && failedFrames > 0) {
            throw new IllegalStateException("所有 OCR 关键帧均处理失败");
        }
```

最后 OCR 文本按 **1 分钟窗口**和语音转写对齐，组装进同一个 `VideoSegment`：

```282:284:server/src/main/java/com/example/server/service/VideoContextService.java
    private long windowStart(long timestampMs) {
        return timestampMs / SEGMENT_MS * SEGMENT_MS;
    }
```

所以一个 60 秒窗口内可能有多帧的 OCR 文本（`ocrTexts` 是 List），以及多张证据图 URL。后续 `VideoChunkingService` 再按 5 分钟聚合这些 1 分钟窗口。

## 两个值得注意的脆弱点

**1. 时间戳可能失真。** 如果 `showinfo` 解析出的时间戳数量少于文件数，会退化成用文件索引估算：

```java
long timestampMs = i < timestamps.size() ? timestamps.get(i) : i * FALLBACK_FRAME_INTERVAL_MS;
```

`i * 30秒` 这个兜底假设"每 30 秒一帧"。但 select 表达式在场景切换密集时实际间隔远小于 30 秒，此时估算会**严重偏移**，导致 OCR 文本被归到错误的 60 秒窗口里。这种偏移不会报错，只会让检索结果悄悄失准。

**2. 整条链路是串行的。** OCR 是 for 循环里逐个 `waitFor` 子进程，没有并发。虽然 OCR 分支整体跑在 `ocrExecutor` 上、和 ASR 分支并行（总预算 60 分钟），但分支内部是严格串行的。2 小时视频如果有几百个关键帧，每次 tesseract 启动 + 识别按百毫秒到秒级算，这里会是耗时大头——也解释了为什么要有 dHash 这一级：**它主要不是为了准确率，而是为了省钱省时间**。

## 参数汇总

| 参数           | 值            | 位置               | 可配 |
| :------------- | :------------ | :----------------- | :--- |
| 场景变化阈值   | 0.35          | ffmpeg select      | 否   |
| 最大采样间隔   | 30 秒         | ffmpeg select      | 否   |
| dHash 汉明阈值 | ≤ 5 跳过      | Java               | 否   |
| 片段对齐窗口   | 60 秒         | `SEGMENT_MS`       | 否   |
| OCR 命令       | tesseract     | `tool.ocr.command` | 是   |
| OCR 语言       | `chi_sim+eng` | `OcrUtils`         | 否   |
| 单帧 OCR 超时  | 2 分钟        | `OcrUtils`         | 否   |